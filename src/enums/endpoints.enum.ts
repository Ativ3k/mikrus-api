export enum MikrusEndpoints {
    INFO = "/info",
    SERWERY = "/serwery",
    RESTART = "/restart",
    LOGS = "/logs",
    AMFETAMINA = "/amfetamina",
    DB = "/db",
    EXEC = "/exec",
    STATS = "/stats",
    PORTY = "/porty",
    CLOUD = "/cloud",
    DOMAIN = "/domain"
}
